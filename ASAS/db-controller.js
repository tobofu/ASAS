// Desc: Controller for database queries
const queries = require('./db-queries');
const { response } = require('express');

const getProfs = (req, res) => {

}


module.exports = {
    getProfs,
};